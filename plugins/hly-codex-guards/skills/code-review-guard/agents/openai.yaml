interface:
  display_name: "Code Review Guard"
  short_description: "Review diffs for real business risks"
  default_prompt: "Use $code-review-guard to review findings only within the current diff or requested scope, while reading all necessary callers, callees, data flow, configuration, contracts, and validation evidence needed to verify changed behavior and realistic counterexamples."
